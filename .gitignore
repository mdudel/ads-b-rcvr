# Maven build output — regenerable, never tracked. Grab release binaries
# from GitHub Releases rather than committing them into main.
target/

# Maven wrapper local install (harmless if unused)
.mvn/wrapper/maven-wrapper.jar

# IDE
.idea/
*.iml
.vscode/
.classpath
.project
.settings/

# OS
.DS_Store
Thumbs.db
